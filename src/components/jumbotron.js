import React from "react";

const Jumbotron = () => (
  <div className="jumbotron">
    <h1>Clicky Game</h1>
    <p>
      Click on any card to start. Don't click on the same one twice!
    </p>
  </div>
);

export default Jumbotron;